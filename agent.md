# 项目代理工作约定

## 2026-09-04 AI 轨迹面板约束

- RTT 对局侧栏必须把 AI 的战略阶段、当前规则窗口、选定战略和有序地点目标作为独立字段展示，不得只显示节点 ID 与通用解释句。
- 当前未完成的第一优先目标必须醒目标识；目标应同时展示原优先级、地图 ID、控制方、资源属性与距东京距离（数据存在时）。
- 历史轨迹保留用于审计，但当前决策摘要始终置顶。

## 项目目标

本项目按四层目标推进：

1. 建立可审计的《太阳帝国》规则知识库。
2. 实现确定性兵棋规则引擎与显式状态机。
3. 将伊拉斯谟（ERASMUS）图表实现为可解释的 PvE 基线 AI。
4. 在同一规则环境上研究基于 Transformer 与强化学习的兵棋 AI。

## 强制工作规则

- 规则事实必须能追溯到来源文件、PDF 页码和规则编号。
- 不允许用推测补齐来源缺失内容；不确定项进入 `docs/rules/qa/open-questions.md`。
- `docs/rules/raw/` 只保存机械提取结果，不进行语义改写。
- `docs/rules/normalized/` 可整理标题、空格和章节，但不得改变规则含义。
- 引擎实现前，规则必须先获得稳定的规则 ID；规则变更必须同时更新测试。
- 状态转移必须确定、可重放；随机性只能来自显式种子和记录在案的随机事件。
- 伊拉斯谟 AI 的每个动作必须返回决策路径、触发条件和对应图表页码。
- 机器人只能读取阵营视图和 `view.actions`，不能读取完整服务器状态或直接修改状态。
- 中文显示层不得改写卡牌、单位、地点和角色的英文规范 ID。
- ZIP 汉化素材必须通过清单记录来源条目、目标对象和 SHA-256，不得覆盖现有源代码。
- 强化学习环境不得绕过合法行动生成器，也不得读取隐藏信息。
- 每次阶段性提交前更新 `plan.md` 和 `havedone.md`。
- 伊拉斯谟图表当前权威文件为 `D:/downloads/伊拉斯谟v2.0_图表汉化 (1).pdf`；页面 JSON 必须记录该文件 SHA-256，旧 Python/自动拓扑不得作为自证来源。
- 提交应保持单一主题；禁止把生成物、临时渲染图和运行日志混入提交。

## 权威性顺序

发生冲突时按以下顺序处理并登记冲突：

1. 用户确认的裁定。
2. 原始英文规则、勘误与官方 FAQ（当前尚未入库）。
3. `太阳帝国（EOTS）V3.2中文规则202402.pdf`。
4. `伊拉斯谟v2.0_图表汉化.pdf`，仅用于 AI 决策，不覆盖基础规则。
5. 当前程序实现与注释，只作为待验证行为，不作为规则权威来源。

## 目录职责

- `docs/rules/`：来源、逐页转写、规范化规则与质检记录。
- `docs/architecture/`：引擎、状态机、AI 边界与数据契约。
- `research/`：研究问题、实验协议、指标和结果。
- `js/`：现有 RTT 游戏逻辑源文件；修改后由项目工具生成编译文件。
- `tests/`：未来的规则单元测试、场景回归测试和 AI 评估测试。
- `tools/rules/`：规则资料提取、校验和索引工具。
- `data/erasmus/`：机器可读图表和策略版本数据。
- `data/erasmus/pages/`：逐页图表 JSON/Markdown；页面文档是图表解释器的权威输入，禁止重新生成占位拓扑覆盖人工修订。
- `docs/localization/`：汉化素材来源、映射和覆盖率记录。

## 完成定义

任务只有在产物、来源追溯、自动检查、人工抽检记录和版本提交都完成后，才可在 `havedone.md` 中标记为完成。

## 伊拉斯谟卡牌与编队约束

- 决策轴只决定当前战略，不得绕过第4/10页卡牌选择图；卡牌用途必须由对应选牌节点决定并传递到后续行动窗口。
- 第5/11页的“最少但足够”同时要求伤害等级、潜在反应兵力和兵种构成达标；夺占目标必须含地面单位，敌控港口/岛屿的两栖突击必须含合法海军护航。
- “最少但足够”只结束当前目标的任务部队，不等于结束整次攻势；剩余合法激活量用于后续目标任务部队，OC 已指定战斗格后则用于未占目标与向下一优先目标前推。
- 图表中的处理框（例如“卡牌分类”“PBM 战略”）是状态机内部节点，不得伪造成引擎动作；必须继续沿显式边走到合法动作节点。
- 反应、潜艇战、撤退和 PBM 的后续窗口必须保留第 6/12 页图表 ID 与真实节点 ID，禁止改挂到任务部队图或通用策略节点。
- 正式自对弈验收不允许通用 fallback、未知节点或静默异常；图表没有合法出口时必须显式暂停并报告缺陷。
- 每个图表节点必须能由 `data/erasmus/node-implementation-map.json` 追溯到引擎入口；此映射证明调用路径，不代替逐节点语义黄金测试。自然语言不得在运行时 `eval`，必须先转换为稳定节点、谓词、选择器和显式边。
- 本地 RTT 统一用 `tools/start-rtt.ps1` 启停；停止前必须核对 8080 监听进程的命令行，禁止按进程名批量终止 Node。
- 日本第3页最终国防圈的驻军全称量词只覆盖日本当前控制的港口/机场；敌控地点不得变成驻军目标。最终防御只能围绕日本区域内的盟军地面单位组织本土兵力，显式空目标链也必须阻断旧南方资源轴。
