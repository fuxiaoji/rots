# 太阳帝国兵棋引擎与 AI 研究计划

## 2026-09-04：对局内 AI 战略可观测性

- [x] 使用现有 `publicTrace.sm` 显示战略阶段和战略名称。
- [x] 将 `priorityTargets` 渲染为有序队列并标记当前首位目标。
- [x] 新轨迹补充引擎阶段与窗口类型；旧轨迹通过图表页码兼容推导。
- [x] 在真实 PvE 对局刷新后做浏览器视觉验收（game 15，盟军视角）。
- [x] 修复 `Balikpapan` 被前缀解析为 `Bali` 的目标污染，并增加精确名称优先回归测试。
- [x] 将保守空优的菲律宾、新加坡、ABDA 从文字标签解析为动态 HQ 位置，并置于东印度压制链之前。

## 总体研究问题

如何在一个规则复杂、隐藏信息显著、行动空间动态变化的卡牌驱动兵棋中，构建可验证的确定性引擎，并比较规则状态机、模仿学习 Transformer 和强化学习策略的表现、稳定性与可解释性？

## 阶段与质量门

### P0 - 项目治理与可复现基线

- 建立 `agent.md`、`plan.md`、`havedone.md`。
- 固定目录职责、来源权威顺序、提交约定和完成定义。
- 质量门：仓库干净、首个项目基线提交可检出。

### P1 - 规则知识库

- 将中文规则书和伊拉斯谟图表逐页 Markdown 化。
- 建立 SHA-256、页码、规则编号和提取方式记录。
- 按 1.0-16.0 章节整理规则书；保留页面重叠以避免跨页语义丢失。
- 将图表分成日本与盟军、决策轴/选牌/任务编成/反应四类。
- 质量门：55 页都有逐页文件；章节文件可追溯；缺页与版面风险有记录。

### P2 - 规则本体与状态模型

- 定义实体：阵营、单位、卡牌、格子、HQ、资源、标记和剧本。
- 定义状态：公开状态、阵营私有状态、随机状态、阶段/环节状态。
- 定义动作：合法性前置条件、成本、效果、后置条件和日志事件。
- 为规则建立稳定 ID，例如 `EOTS-7.25`。
- 质量门：状态 schema 可序列化、可哈希、可重放；核心规则有决策表。

### P3 - 确定性状态机与规则引擎

- 实现回合、阶段、环节和中断/反应窗口。
- 分离合法行动生成、状态转移、随机数和视图投影。
- 用固定种子记录完整事件流，支持保存、恢复、回放与差异比较。
- 质量门：核心流程与战斗测试通过；同一状态、动作和种子产生相同结果。

### P4 - 伊拉斯谟 PvE 基线 AI

- 将 12 页图表转成机器可执行决策图。
- 实现日本与盟军的阶段切换、选牌、任务编成和反应决策。
- 所有动作输出 `decision_trace`，包含节点、条件、掷骰、候选动作和最终选择。
- 质量门：逐节点黄金测试覆盖；图表回放结果与人工走图一致。

### P5 - 训练环境与数据集

- 提供 Gym 风格 `reset/step/legal_actions/observation` 接口。
- 用伊拉斯谟、自博弈和人工对局生成带版本号轨迹。
- 隐藏信息按玩家视角投影，杜绝信息泄漏。
- 质量门：轨迹可重放；数据 schema、种子、引擎版本和策略版本齐全。

### P6 - Transformer 与强化学习

- 先做行为克隆基线，再做离线 RL/在线自博弈。
- 候选架构：状态/事件序列编码器 + 合法动作掩码 + policy/value heads。
- 对照组：随机合法策略、启发式策略、伊拉斯谟策略和搜索增强策略。
- 质量门：在冻结评测集和未见种子上报告胜率、非法动作率、决策时间和校准误差。

### P7 - 科研评估与报告

- 预先固定主要指标、对照组、随机种子和停止条件。
- 分离探索性实验与确认性实验。
- 报告消融：规则特征、历史长度、动作表示、奖励塑形和对手池。
- 质量门：结果可复现；结论不超出数据；限制与失败案例完整记录。

## 当前迭代

当前迭代：12 页已生成独立 JSON/Markdown 文档与运行时编译数据，图表解释器已接入 South Pacific；条件谓词仍有显式 inferred 项，必须继续逐页视觉校对和黄金路径覆盖。未经校对的图表条件不得伪装成确定规则。

配套补全（2026-09-03）：
- `erasmus_complete_ai_state_machine.py` 作为可运行的确定性状态机 AI 研究参考（覆盖决策轴/选牌/编成/反应四窗，含 `--self-test`）。
- JS 引擎 `js/server/bots/erasmus.js` 解释器升级为“按候选迭代 + 合法动作过滤”的优先级解析；`*_OPS_CARD` 与 `*_EVENT_CARD` 在选行动窗口消歧；策略版本 `erasmus-v2.0-zh.3`，50 固定种子回归 0 fallback、全部正常终局。
- 1942 完整剧本自对弈：修复 `before_commit_offensive` 卡牌攻势限制导致的“确认窗仅剩 undo”死锁——受限确认窗新增 `cancel` 出口，回退到选行动窗口并将该卡本回合禁止再以攻势打出；新增剧本参数化 AI vs AI 运行器，`1942-1945 (The Shortened Campaign)` 10 局 0 报错、全部正常终局。
- 攻势零会战修复：`evaluateChart` 里“unit+done 且有激活单位即强制 done”的兜底不再作用于 `Declare battle hexes.` 窗口——该窗口的 `unit` 是选取射程内已激活空中单位发起空袭（随后 `action_hex` 创建战斗格），此前被整窗吞掉导致全程零会战。策略版本 `erasmus-v2.0-zh.4`；1942 全剧本 10 局 0 报错、每局平均申报 16.5 个会战格并产生真实交战，South Pacific 50 种子回归 0 fallback/全部终局。已知边界：无头 bot 无法做地面接敌移动（目标路径由客户端 `move(path)` 提供，服务端不暴露路径参数），会战以空中打击为主。
- RTT PvE 场景白名单扩到 `1942-1945 (The Shortened Campaign)`（`create.html` 不再强制回南太平洋），供人类在浏览器里亲自体验完整剧本 vs 伊拉斯谟；服务端建局冒烟通过。
- 50 局多种子完整剧本验证（seeds 20260903–20260952，审计运行器 `tests/erasmus-campaign-audit.js`）：发现并修复 **Fuel Shortage 事件窗死锁**（`events.js` `P.fuel_shortage`：选中单位无可落位目的地时窗口只剩 undo，真人靠 undo、bot 无合法动作）——`prompt()` 检测陷阱自动丢弃该次选择并写入 `L.unmovable` 不再重复候选；引擎级语义等价出口，策略版本仍 `erasmus-v2.0-zh.4`。修复后 50/50 正常终局、0 报错；胜率日本 50 / 盟军 0；双方战略（决策轴+选牌）与战术（编成+反应+空袭会战申报+交火）决策均 >0（日本 49.9%/50.1%，盟军 46.6%/53.4%）。SP 50 与 1942 10 局回归与基线逐项一致。
- 无头自对打环境修复（策略版本 `erasmus-v2.0-zh.5`）：地面/海上接敌移动路径原由客户端算（`L.allowed_hexes` + `move(path)`），服务端不暴露路径动作参数，无头 bot 无法推进进敌格。新增 opt-in `headless_moves` 开关，`P.move_offensive_units` 在攻击/会战移动/反应三阶段复用客户端 `update_move_hex()` 计算落格并按目标评分执行 `advance`（推进/夺格/进战斗格），解除 PBM 无路可走悬死、补 `js/move.js append_path` 潜伏 `units` 未定义 bug、`erasmus.js` 加 `advance` 覆盖与 awaiting-only 兜底。50 局完整剧本（headless 开）50/50 终局 0 报错、260 次地面接敌、双方各 1322/1911 次推进决策、盟军夺格 22→177；headless 关的 OFF 50 / SP 50 / 1942 10 三组回归与 zh.4 基线 0 差异（行为 opt-in，关闭不变）。胜负仍日本全胜——阵营失衡为已知范围外，非本修复对象。
- 盟军 0 胜定向归因（headless 10 局，seeds 20260903–20260912）：10/10 以 **PW 归零条约败** 结束（t8–11），~2/3 扣分是 Progress of War 未达标（逐回合需新夺 ≥pow 个名城格，几乎每回合失败，10 局 ~67 败 vs 2 成）；夺格吞吐太低且散——马来亚/暹罗同几格的反复拉锯（Kuala Lumpur×11/Jitra×8）占 29/44，中央太平洋只偶达 Saipan/Palau，**从未触达菲律宾马尼拉/冲绳/日本本土任何格**（0 例），故“登陆日本失败率”实为“从未能发起登陆”；~50% 攻势无战事（No battle hexes declared）。根因是图表解释器只有分窗战术、无回合级选轴与夺格配额。修复方向（拟 zh.6）：回合级“保 PoW 夺格节奏 + 中央太平洋主线 + 过滤空转攻势”。
- zh.6 已落地其“选轴 + 焦点执行”子集（`erasmus-v2.0-zh.6`）：新增 `js/server/erasmus_ops.js` 目标聚焦操作层（盟军中太平洋主线 Wake→Tarawa→Kwajalein→Eniwetok→Palau/Ulithi→Saipan→Iwo/Okinawa→本土；日本资源<13 时南方资源夺控轴），`erasmus.js` 选目标格/会战单位与 `offensive.js` 可渡海推进都向“当前最优先未夺目标（焦点）”聚焦、目标夺控才放行下一格，publicTrace 增 axis/focus 可审计；同源 Python 参考引擎 `erasmus_complete_ai_execution_engine.py` 通过 `--self-test`。headless 完整剧本 50 局 50/50 终局 0 error（含修复“无进攻路径撤退单位”崩溃 2 种子）；同种子 10 局 No-battle 申报 343→278、马来亚/暹罗拉锯消失、日方南方资源轴依目标表夺控；胜负仍日本全胜——回合级“保 PoW 夺格节奏”与盟军两栖登岛推进留 zh.7。
- zh.7 完整状态机移植（`erasmus-v2.0-zh.7`）：按用户指示把完整 py 参考引擎 `erasmus_complete_ai_execution_engine.py` 的**决策树 + 有序目标链 + 阶段门槛** 1:1 移植成 JS 回合级状态机 `js/server/erasmus_state.js`——每方每游戏回合的首卡窗按 py 树求值一次、钉住该方该回合战略，下回合首卡重评；仅完整全图剧本启用（gate 需地图含菲律宾/DEI/日本区域；South Pacific/Burma 等 gate-off 走原 zh.6 路径，SP-50/erasmus.test.js 金标不动）。目标链经 `eop_set_strategy_chain` 覆盖固定轴，其余沿用 zh.6 焦点执行层。**保真自测** `tests/erasmus-state-fidelity.test.js`：59 例（含 py `--self-test` 三黄金用例）同 ctx 同策略名，逐字通过。headless 完整剧本 50 局（seeds 20260903–20260952）50/50 终局 0 error/0 action-limit、968 次钉选（≈19.4/局 = 每方每回合首卡一次），轴分布 JP 早期外围防御/中期资源战略、AP 早期建立ABDA/t4 DEI/t5-8 南太平洋/t9+ 占领轰炸基地，阶段门槛随真实地图状态切换。同种子 10 局 zh.6→zh.7：盟军夺格 14→23、AP 推进决策 131→357、groundMove 24→45、fallback 28→19、no-battle 278→249、日本转图表守势（capturedJP 10→0）。胜负仍日本全胜：盟军 50 局仍未夺马尼拉/马里亚纳/冲绳/日本本土名城格——两栖登岛/夺岛推进瓶颈留 zh.8。
- zh.7 B 引擎占格/登岛执行（同一 `erasmus-v2.0-zh.7`）：先修夺格计数低估（早期探针只扫本方 action 增量、漏 ~8 成跨方 `done/next` 边界结算与 `&A` 会战窗前缀的夺控行，曾误报“基线 0 夺岛”）；真根因 = 攻势总挑离焦点最近的纯空/海军（只空袭不夺控）+ 两栖编成不渡空海（够不着焦点岛就原地放弃）。修复两处执行：`erasmus_ops.js` `eop_pick_unit` 焦点敌占时偏置激活两栖地面（B_BIAS）、`offensive.js` 两栖编成朝焦点空海巡航推进（B_CRUISE）。同种子 40 局 A/B（20260903–20260942，逐格夺控事件口径，均 0 error/action-limit）：AP 夺格 193→497（4.8→12.4/局）、触岛种子 26→39/40、岛屿夺控 70→290；纵深恩尼威托克 5→33/40、硫磺岛 8→32/40（真实 `+3 Amphibious assault` 强攻登岛）。边界：马尼拉/冲绳/本土终局控制两臂仍 0/40——剧本 ~t8-12 日本 PW 先胜，冲绳/本土不在时间窗；详见 havedone。
- zh.7 A/C 忠实补做（同一 `erasmus-v2.0-zh.7`，用户“研究一下,状态机原文是没问题的,可能是你偷懒了”指示下对 py 参考引擎做缺省部分逐字对拍）。**(A) 计划层忠实化**：补移植 py `_resolve_pointer`（L857-871：检索序 JP_MID→JP_EARLY→JP_LATE→AL_MID→AL_LATE→AL_EARLY、`token∈key or key∈token`、跨 Goal 扁平化**不去重**、取链最长者）与逐行 `_classify`；修“见外围防御”错落 13 格兜底（1/38 失配根因：角色标签与指针递归去重两处），py 金标 `tests/results/py-goals-golden.json` + `tests/erasmus-goal-fidelity.test.js` 逐策略比 kind 序列与逐位 hex——**39/39 逐字相等**。**(C) 事件战略顺序化**：py 对 EVENT 只在 executor ADMIN-log 不打牌，事件清单（JP 8 行 / AL 6 行）是“按序打事件”的行为说明——JS 钉住事件战略时按 py 口径展开（JP 中/晚目标“同早期阶段事件战略”为指针、AL mid/late 决策树直接 `return AL_EARLY_STRATEGIES["事件战略"]` 且 JS AL mid/late 库原无此键曾静默落空 EVENT）；`esm_bind_strategy_entry` 把事件战略任意阶段绑定到【早期】清单条目，选牌窗 `esm_event_strategy_card_pick` 把清单逐行译成引擎条件按序执行（结束己方 ISR=己阵营 `isr_agreement` 牌且己方 ISR 激活；造成敌方 ISR=己阵营 `isr_rivalry` 牌且敌方未 ISR；东京玫瑰/杜立特/巴丹/天气点名牌），命中行内取最小 OV、行不可行顺延、全行无命中退通用选牌；`via=事件战略:清单#N「行」` 与 `eventPhase` 进 trace 可审计。自测 `tests/erasmus-event-strategy.test.js`（JP/AL×三阶段→早期 8/6 行）8 例过；headless 1942 16 局 0 error、事件战略钉选 goals=8、定向命中例 `清单#3「造成美国ISR」`/`清单#4「东京玫瑰」`。gate-off（erasmus.test.js / SP）与 goal-fidelity 39/39、state-fidelity 59/59 全绿。
- zh.7 D1–D5 驱动层补全 + 胜利线对账（同一 `erasmus-v2.0-zh.7`，用户“盟军 js 状态机漏了什么、为什么还是输”指示下继续审计）：决策树/A/C 层已 1:1 忠实，漏在驱动层反馈与引擎胜利线。**D1 批内跨局串扰**：`esm_clear_cross_game` 每新局清 EOP_OVERRIDE/重算（同种子批内第 2 局曾 T10 vs T12 漂移）。**D2 同轴延续**：同阶段换 d10 不再每回合重滚对翻轴（seed20260909 中太平洋↔CBI 交替空转 → 连续 2 回合同轴 run），钉住的战略在其目标窗口内走满才换轴。**D3 真实 PoW/反攻信号**：`al_M_B` 由“PoW 目标已定”改为“PoW 银行（AP 仍控 G.capture 名城格数）< G.pow”真实感知缺夺格节奏；`al_M_D` 回归 py 字面谓词（反攻清单 16 目标任一 JP 控，与 eop 执行链同源）——此前为让反攻早点触发而放宽成前线扫描，反致“触发但清单目标全在己手、eop 焦点 null 空转”；回退后 seed20260924 反攻真钉南太平洋 891、T5 后推进 B29。**D4 ABSTRACT 操作化**：推进B29/原子弹胜利 钉住期间按图改打 OC/事件（苏联入侵满洲等可用优先）、空链落 占领轰炸基地/重返菲律宾 可执行回退链。**D5 胜利线对账**：ctx._diag 逐回合账本（pow/bank/jpRes/marker 进 decision trace）；引擎级修复 `STRAT_BOMBING_CAMPAIGN` 标记语义——原 `check_event` 存 G.turn（首次成功轰炸回合号），1942-45 中 B29 第 9 回合才增援、最早 T10 首炸，标记恒 ≥10，而 `victory_1945` 判 `1≤标记≤9`，故“日本因战略轰炸投降”规则层不可达（多种子实测 mk=10、res 降到 3 亦不投降）；game.js bombing() 不再设该事件、cycle.js roll 改为“连续成功轰炸段计数 1..9 封顶”，胜利线可达。终局冲刺轴（登陆日本/原子弹胜利）把仍 JP 控的资源格按距链首近者前置入链（资源剥夺，含朝鲜/满洲/南方格）。回归全绿：state-fidelity 59/59、goal-fidelity 39/39、event-strategy 8/8、erasmus.test.js（gate-off）。headless 1942 10 种子（20260903–20260912）结局 5 treaty / 5 撑到 T12 “Japan did not surrender”（引擎判负），仍 0 盟军胜：res 实测卡 3–5 从不下 ≤1，且部分种子在 占领轰炸基地 钉到 T12 未达 登陆日本/原子弹胜利、资源剥夺门救不到它们；盟军要胜需 ~T10 前 B29/轰炸基地成型并打到 res≤1，属剧本时间窗/执行强度边界，详见 havedone。

## 初始评估指标

- 引擎：规则测试通过率、重放一致率、非法状态率、每步耗时。
- 伊拉斯谟 AI：图表节点覆盖率、人工一致率、非法动作率、决策可解释率。
- 学习型 AI：胜率、Elo、样本效率、动作熵、价值校准误差、跨剧本泛化。
- 工程：可复现实验比例、规则到代码追溯覆盖率、回归缺陷数。

## 当前增量：zh.8 原子弹战略标准与可观测性（2026-09-04）

- [x] 将图表 09 的三项原子弹标准固化为引擎/AI 共用谓词，并按用户裁定接入 `victory_1945`。
- [x] 修复苏联牌可打真值、东京 8 格机场、地图上 B29、东京 8 格受控六角格四处真实状态映射。
- [x] 修正 page-09 JSON 的 G 节点拓扑与三项结构化条件，更新生成器及运行时图表数据。
- [x] RTT 每回合战略钉选输出阶段、战略、动作、焦点、优先目标、推进距离、原子弹条件。
- [x] 扩展自对弈审计数据并以相同 50 个种子复测；产出逐局 JSON 与分析报告。
- 验收结果：50/50 正常终局，0 error / 0 action-limit；盟军 2 胜（4%），均为原子弹战略投降。

## 当前增量：zh.9 实际图表复刻与 20 局验收（2026-09-04）

- [x] 用新 PDF 逐页视觉核验 12 页，建立 schema v3 页面 JSON、稳定节点 ID、显式边、0–9 骰表、PDF SHA-256 和对应 Markdown。
- [x] 六张战略轴改走实际节点路径；每次选牌重新求值，移除两回合连续性和终局动态资源前插。
- [x] 卡牌分类、任务部队、反应和 PBM 分图；任务部队加入最低兵力估算，恢复完整战役航空兵激活。
- [x] 未知谓词改为显式异常；状态机异常不再静默降级；轨迹校验实际节点。
- [x] 修复反应分配、申报会战、HQ 可变激活上限和不可落位增援的四类死锁。
- [x] 固定种子 20 局验收：20/20 完赛，0 error/action-limit/fallback/未解释空攻势/缺失轨迹节点；日本 19、盟军 1。
- [ ] 后续结构重构：把历史 `erasmus_state.js`/`erasmus_ops.js` 的全局状态读取全部替换成纯 `view.ai` 输入，并把宿主 AI 轨迹缓存纳入跨进程存档恢复测试。

## 当前增量：zh.20 后方机动、格外航空与目标集中（2026-09-05）

- [x] 撤销“所有日本早期战略必须完成菲律宾/东印度投降”的非图表硬覆盖，投降仅作诊断。
- [x] 修复航空单位激活后被送回回合轨，恢复基地外按战斗航程参战。
- [x] 为过远航空和非航空后备队接入合法扩展航程/战略移动。
- [x] 盟军 PoW 目标优先保留在当前决策轴目标链，降低跨战区分散。
- [x] 撤退 `eliminate` 出口归入第6/12页真实反应节点。
- [ ] 增加逐目标失败原因统计（不可达/兵力不足/战败/被反夺），用于下一轮强度改进。

## 当前增量：zh.10 卡牌路由与任务部队热修（2026-09-04）

- [x] 复现 game 16：日本持有多张可执行军事事件，却由决策轴 `CONQUEST` 直接选反应牌作为 OC。
- [x] 日本选牌重新执行第4页 E 节点：手牌多于2张且有可执行无限制军事事件时，选择最高后勤值事件并在下一窗口按 EC 打出。
- [x] 为全部战略目标补充 `SUPPRESS/CONQUEST` 元数据，避免普通敌控岛屿丢失“必须夺占”语义。
- [x] 编队完成判定加入减面战力、潜在反应兵力、地面夺占与海军护航；两栖编队按护航→地面单位形成。
- [x] 新增卡牌/编队定向回归测试，并重建运行时图表与内联规则产物。
- [ ] 在新的实战攻势中观察 EC 使用、多人编队和两栖登陆结算日志。

## 当前增量：zh.11 多目标任务部队与激活量利用（2026-09-04）

- [x] 压制目标改按敌方 AZOI 是否仍覆盖判定完成，不再错误要求占领 Jolo 等压制地点。
- [x] 当前目标达到最低编队标准后继续使用剩余合法激活量，为后续目标编队或前推后方单位。
- [x] 敌控夺占目标继续强制地面单位和海军护航；压制目标明确标注“不要求占领”。
- [x] 调试轨迹显示激活上限、已用/剩余数量、编队需求、现有兵力和潜在反应兵力。
- [ ] 运行完整战役多局验收，统计平均激活率、多战斗格率和两栖夺占率。

## 当前增量：zh.12 双方 12 页状态机闭环（2026-09-04）

- [x] 将 1–3、7–9 页战略轴从自动线性拓扑改为与视觉箭头一致的显式条件、骰表和出口。
- [x] 将第 4/10 页卡牌分类处理框接入每次选牌，将选牌意图传递给 EC/OC/FOQ 执行窗口。
- [x] 将第 5/11 页任务部队处理框接入激活、移动、会战和多目标编成；修复 HQ 加成反复变化造成的激活循环。
- [x] 将第 6/12 页反应、潜艇、撤退及航空/海军/失败两栖 PBM 分别路由到实际页面节点。
- [x] 航空 PBM 实现六级优先目标、一机场一空军和最强空军优先；海军与失败两栖 PBM 使用各自港口优先级。
- [x] 反应实现情报牌优先、战斗格优先、双方不同兵力标准，以及 CV→BB→CA→DD 的潜艇损失分配。
- [x] 12 页 JSON 均为 `visual_review_required=false`、`inferred_nodes=[]`，生成运行时图表和 Markdown。
- [x] 逐节点、目标链、选牌、编队、保存恢复和确定性测试全部通过。
- [x] 固定种子 20 局验收：20/20 完赛，0 error/setup-error/action-limit/fallback/未解释空攻势/缺失轨迹节点。
- [x] 统计航空/海军/失败两栖 PBM 六条路径均有实际覆盖；双方战略轴、选牌、编队和反应四类图表均有实际决策。
- [ ] 后续安全重构：历史状态派生函数仍运行在规则模块内部并读取 `G`；需要把这些派生量固化为不可变 `view.ai` 快照，作为独立的信息隔离里程碑。该项不影响本轮合法动作闭环验收，但在隐藏信息安全验收前不得标记完成。

## 当前增量：zh.13 CDSS 脚注落实（2026-09-05）

- [x] 全量提取日军 259 条、盟军 424 条提示板正文，保存来源路径、SHA-256、段落号和所属图页。
- [x] 生成 258 个图表节点的区域坐标回填 CSV；坐标保持空白，等待人工输入，禁止再用整页占位冒充节点区域。
- [x] 修复 JP02-E 将美国政治意志误读为欧洲战事值的错误。
- [x] 事件战略补充欧战正负、ISR 不适用时转 FOQ、天气转 FOQ、东条 1OC 和其他牌转 FOQ 的执行语义。
- [x] 卡牌选择排除不满足条件的 Operation MI，并加入后勤值后的奖励优先级。
- [x] 占领战略轰炸基地强制选择当前最大有效 EC/OC 攻势卡。
- [x] 任务部队将所有可反应海空军纳入伤害标准，并落实非日本/非印度 HQ 至少保留一个未激活地面单位。
- [ ] 继续逐条实现最终防御、一亿玉碎、精确 2x 生存概率、伏击 DRM 和反应兵力 D10 差异；每项必须有独立黄金测试后才能关闭。

## 当前增量：节点实现索引与本地运行文档（2026-09-05）

- [x] 从 12 页图表自动生成 258 个逻辑节点到引擎处理器的映射，区分专用实现、通用骰表和图表族公共控制节点。
- [x] 编写自然语言到稳定节点、谓词、目标链、候选规划器和合法动作的转换规范。
- [x] 编写 Windows 构建、启动、健康检查、日志、停止与重启指南。
- [x] 增加可直接运行的 `tools/start-rtt.ps1`，支持 Start/Stop/Restart/Status 与可选 Rebuild，并保护非 RTT 的 8080 监听进程。
- [ ] 继续由人工在 `node-regions.csv` 回填 PDF 节点视觉坐标；逻辑实现索引不能推导视觉位置。

## 当前增量：zh.14 最终国防圈与本州最终防御（2026-09-05）

- [x] 复盘 game 18 的战略轨迹，确认最终国防圈把敌控冲绳计入驻军全称量词，导致第3页 BC 永久为假。
- [x] 驻军检查只过滤日本仍控制的港口/机场；敌控地点既不算缺驻军，也不转换为夺回目标。
- [x] 最终防御从日本区域内的盟军地面部队动态建立本州目标，单位选择限制为本土兵力并按地面→空中→海军支援排序。
- [x] 显式 GARRISON/DEFEND/EVENT 空目标链阻断旧日本资源轴；本土防御无合法焦点时禁止通用远方攻击。
- [x] 防御战略在卡牌树没有明确事件出口时使用 OC，进入激活、移动与会战流程。
- [x] 新增最终国防圈、本州目标、兵种驻军、空链阻断和防御 OC 的回归测试。
- [x] 重建内联产物并通过全部 Erasmus 测试与完整战役冒烟（seed 20260903：正常终局、0 fallback、0 缺失节点）。
- [x] RTT 已热重启到新规则（PID 34988，HTTP 200）；现有 game 18 可刷新后继续，后续 AI 动作使用 zh.14。
- [ ] 浏览器实战继续观察属于人工验收；若旧轨迹仍显示冲绳/资源格，先确认该条动作是否发生在热更新前。

## 当前增量：zh.15 胜利线前视与 AI vs AI 复盘观战（2026-09-05）

- [x] 读取 game 18 最后 80 条规则日志与回放，确认第8回合 PoW 要求4、实际保持3，政治意志1→0后触发条约谈判日本胜；最后 C49 弃牌来自人类盟军席位，不是 AI。
- [x] 梳理缩短战役盟军胜利路径：避免政治意志归零；占领日本本土、连续3回合切断本土资源线，或第12回合满足原子弹战略标准。
- [x] 加入盟军 PoW 生存前视：政治意志仅1且 PoW 未达标时，用最大合法攻势替代 FO/弃牌，不改战略轴目标与引擎合法动作。
- [x] 保护已在东京8格内友军机场就位的 B29，避免普通攻势把它送回回合轨而错过战略轰炸。
- [x] 新增 AI vs AI 建局模式：双方均为伊拉斯谟，创建者以 Observer 进入，默认暂停。
- [x] 新增“AI 下一阶段”控制：一次执行双方在当前阶段内的全部行动、反应、会战与 PBM，并在下一个 `@Turn N.*` 阶段标题暂停。
- [x] 固定20局验证：20/20 完赛、0 error/fallback/缺失节点；12局撑到第12回合，但仍为日本20胜，说明合法性与生存略有改善，胜利转化仍不足。
- [ ] 热重启 RTT，实际创建一局 AI vs AI 并验证单步按钮与阶段暂停。

## 当前增量：zh.16 目标硬优先与空攻势修复（2026-09-05）

- [x] 从 game 19 的 474 条日军轨迹确认：图表焦点长期为 Manila [2813]，但激活排序仍把“距任意弱敌最近”置于“距焦点最近”之前，移动评分也先挑弱守军，导致兵力转向婆罗洲/小岛并出现空攻势。
- [x] 有明确图表目标时，激活候选改为先按目标距离排序；任务部队同兵种优先本轮更接近目标的单位，不再为远方高战力牺牲可达性。
- [x] 无头攻击移动把当前图表焦点设为硬优先：占领目标要求地面编成；只有焦点对该编队不可达时才选下一合法敌格。
- [x] 新增马尼拉目标回归，覆盖“近弱敌的远方陆军”与“靠近焦点的可用陆军”之间的选择。
- [ ] 重建产物、全量测试、重新热启动，并以新 AI vs AI 局验证单步暂停和菲律宾行动。
- [x] 修复目标轴对保存/复盘的隐式依赖：`advance` 将焦点、目标类型和占领要求写入动作参数，连续运行与动作回放保持一致。
- [x] 纯地面前推在存在明确目标时也按目标距离转向，覆盖仰光/印度等陆路战线；无目标时仍保留安全的就近规则。
- [x] PBM 保持合法单位范围不变；在图表列明的六级/三级优先均相同后，以距下一战略目标作为平分键，减少航空与海军反复退到后方。
- [x] 盟军 PoW 危机前视从政治意志1扩展到≤2，提前一回合阻止 FO/弃牌造成的致命空窗。
- [x] 重建 12 页图表、节点实现索引与内联运行产物，并通过全部 Erasmus 回归测试。
- [x] 固定种子 20 局（20260903–20260922）最终验收：20/20 正常结束，0 error/setup-error/action-limit/fallback/未解释空攻势/缺失轨迹节点；日本 20 胜、盟军 0 胜，10 局进入第12回合。
- [x] 记录推进/夺控指标：232 次地面推进；日志中的 `AP captured` 82 次表示盟军夺格，`JP captured` 48 次表示日军夺格。胜负结果如实保留，不用图表外加成追求 50/50。
- [ ] 下一里程碑针对盟军胜利转化做规则内审计：政治意志收益、两栖目标可行性、战略轰炸连续性与日本资源线切断；必须逐项有规则/图表依据后再改动。

## 当前增量：zh.21 盟军终局图纠错与双胜利路线（2026-09-05）

- [x] 对照用户提供的第9页图，修正 B=YES 直接进入 F 的箭头及第12回合黄金路径。
- [x] 原子弹胜利执行框改为“苏联入侵满洲事件优先 + 精确选择两个剩余日本资源格”，移除错误的重返菲律宾整链回退。
- [x] 为第6回合后的盟军加入透明的 B29 基地前置层；PoW 亏空时仍优先本牌可达的近程计分格。
- [x] 加入日本资源线封锁层：北方上海/青岛/旅顺，南方台南/台北/冲绳/硫磺岛/塞班/关岛；夺取后要求航空驻军形成 AZOI，并读取真实三回合断线计时。
- [x] 进攻激活窗过滤 HQ，审计新增双方 HQ 空激活与三类胜利准备计数。
- [x] 固定20局技术验收：20/20结束、0异常/上限/fallback/未解释空攻势/缺失节点、双方 HQ 空激活均为0。
- [ ] 战力验收仍未通过：盟军0胜，终局平均仅保有1.2个 PoW 格，日本平均仍控6.95个资源格；需继续修正中期夺格与守岛转化，不能仅增加终局目标权重。

## 当前增量：zh.22 战区集中、后方空军与东印度目标精确化（2026-09-05）

- [x] 固化 game 27 的完整公开轨迹与回放，并输出回合级政治意志、PoW、盟军距东京推进和日本资源格摘要。
- [x] 修复美国海军落位被仰光地面战线吸引的问题；非 CBI 战略优先当前太平洋目标附近合法港口。
- [x] 修复中文战略名未被 HQ 选择器识别、几乎总退回 Central Pacific HQ 的问题。
- [x] 排除无法通过一次移动进入当前目标战斗航程的远后方 HQ 航空兵，防止夏威夷折返跑。
- [x] 禁止无日本本土图表焦点的盟军通用攻击选择日本本土战斗格。
- [x] PoW 前视严格限定在当前决策轴目标，不再把哈尔滨、奉天等全图弱目标插入主轴。
- [x] 第1页东印度投降表结构化为四级目标；Batavia 执行“爪哇无其它日军地面部队”脚注，且与 Jolo 等压制目标隔离。
- [x] 重建图表、实现索引和内联产物；全部 Erasmus 自动测试通过。
- [ ] 新策略的盟军胜例尚未取得：首轮50局仍为日本50胜，需继续核查苏联牌保留/重洗和原子弹资源转化，不能伪造胜例。

## 当前增量：zh.18 日本开局任务部队与格外空海支援（2026-09-05）

- [x] 复核引擎格外参战路径，确认航空兵和有战斗航程的航母无需进入战斗格。
- [x] 修正受限事件分类，加入真实启动区、补给、OOS、事件兵种与 HQ 可用性预检。
- [x] 日本第2回合占领目标只把能形成占领、护航和远程支援编成的 EC 当事件使用。
- [x] 首要目标宣战后，后续任务部队沿图表链尝试下一目标。
- [x] 增加 Turn 2 固定种子审计及卡牌、HQ、多目标回归测试。
- [ ] 继续消除剩余 8 次/20 局日本空启动与 2 次无护航失败；7/20 东印度夺控尚不能视为开局战略完全合格。

## 当前增量：冻结 AI 1.0 + 测试版 8 项回归（批次 1–4 + 决策轴重判，2026-09-06）

- [x] 冻结 a68de77（zh.22）为 `erasmus-v1.0`「AI 1.0」；测试版改名「测试版」；建局页双版本/跨版本对战。
- [x] 批次1：#1 后方调度硬过滤移除 + #4 战后移动补 SR。
- [x] 批次2：#8 占领 met 纳入地面 + #3 兵力不足提前 done 空攻势。
- [x] 批次3：#2 占领地面 1:1 门槛（修 damageLevel 误除）+ #6 航空转场前推降权（修夏威夷折返跑）。
- [x] 盟军胜利路线：苏联牌权重 + 原子弹不可达转封锁 + 釜山/汉城封锁目标。
- [x] 50 局审计·批次3后：盟军 1 胜（原子弹胜利）/ 日本 49 胜，0 异常（验收「盟军至少一局获胜」达成）。
- [x] 50 局审计·决策轴重判+批次4后：盟军 0 胜 / 日本 50 胜（atomicBombWins=0），capturedAP=229 / capturedJP=178、groundMove=557，0 异常/fallback/未解释空攻势。空优→资源过渡令日军更忠实夺占，但原 1 胜是 2% 单局小概率，轨迹偏移后未复现，盟军胜利转化仍不足。
- [x] 决策轴重判（用户反馈「重大bug」）：每张牌重判与阶段切表已确认原生可用（诊断实测）；本回合后勤值 `esm_jp_logistics` 按 (sid,turn) 缓存，避免手牌变化令后勤值逐牌抖动翻转策略；空优战略（保守/激进）的压制目标按「无敌方 AZOI」口径全部达成后，重判转入南方资源战略实际夺占（菲律宾/马来亚/东印度投降），修复「空优执行完就没目标」空链停滞。
- [x] 批次4：#5 首目标不可行遍历下一目标（已于 57cc54f 批次1完成）+ #7 己方 ISR 激活时全局前置打 `isr_agreement` 和解牌清除（不限 EVENT 战略轴，避免 CONQUEST/ABSTRACT 轴把和解牌当 OC 打掉）。
- [ ] 日军前期陆军不上马尼拉：已由「空优达成→转南方资源」过渡部分缓解（空优不再永久停滞），但资源战略下马尼拉仍未夺占——动态 SUPPRESS_HQ 目标顶替了「菲律宾投降」CONQUEST 目标（同一 hex 去重），压制达成后无夺占意图；用户指示不改菲律宾投降权重，此项留待确认。
